/**
 * Task Planner API starter.
 * Build REST resources, validation, auth, and persistence.
 */
function createApp(config = {}) {
  throw new Error("TODO: implement createApp");
}

module.exports = {
  createApp,
};