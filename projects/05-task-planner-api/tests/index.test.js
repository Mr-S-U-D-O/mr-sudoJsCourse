const { createApp } = require("../src/index");

describe("task-planner-api", () => {
  test.todo("supports authenticated CRUD for tasks and projects");
  test.todo("validates request payloads and query params");
  test.todo("applies authorization checks by role/ownership");
  test.todo("supports filtering, sorting, and pagination");
});