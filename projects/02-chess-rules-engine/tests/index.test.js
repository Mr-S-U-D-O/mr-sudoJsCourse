const { createGame } = require("../src/index");

describe("chess-rules-engine", () => {
  test.todo("validates legal moves for each piece");
  test.todo("rejects moves that expose own king");
  test.todo("supports castling, en passant, and promotion");
  test.todo("detects checkmate and stalemate");
});