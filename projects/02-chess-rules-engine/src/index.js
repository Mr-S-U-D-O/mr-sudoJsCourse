/**
 * Chess Rules Engine starter.
 * Build legal move validation and deterministic state transitions.
 */
function createGame(initialState) {
  throw new Error("TODO: implement createGame");
}

module.exports = {
  createGame,
};