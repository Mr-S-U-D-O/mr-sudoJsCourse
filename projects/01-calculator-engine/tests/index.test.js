const { evaluateExpression } = require("../src/index");

describe("calculator-engine", () => {
  test.todo("evaluates expressions with operator precedence");
  test.todo("handles nested parentheses correctly");
  test.todo("returns descriptive errors for invalid syntax");
  test.todo("rejects unsafe evaluation strategies");
});