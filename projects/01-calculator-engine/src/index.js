/**
 * Calculator Engine starter.
 * Build parsing, validation, and evaluation without using eval.
 */
function evaluateExpression(expression, options = {}) {
  throw new Error("TODO: implement evaluateExpression");
}

module.exports = {
  evaluateExpression,
};