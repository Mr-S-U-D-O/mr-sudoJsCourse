/**
 * Fill the blanks using your understanding of `const`.
 */
function completeConstBlanks() {
  return {
    // const is ____ scoped.
    scopeType: "",

    // Can const be reassigned? true/false
    reassignmentAllowed: null,

    // const requires immediate ____
    declarationNeeds: "",

    // Can properties inside const object change? yes/no
    objectPropertyMutation: "",
  };
}

module.exports = completeConstBlanks;
