# 03-const

## Learning First
This lesson is teaching-first. Start with `lessons/` and use `challenges/` for lightweight practice.

## Folder Guide
- `lessons/`: explanation, visualizations, and resources
- `challenges/`: optional mini code-completion challenge and test

## Mini Enrichment
- Find one place where `let` can become `const`.
- Explain "binding immutable, value mutable" in one line.
- Draw a quick memory model for `const obj = {}` and `obj.name = "A"`.
