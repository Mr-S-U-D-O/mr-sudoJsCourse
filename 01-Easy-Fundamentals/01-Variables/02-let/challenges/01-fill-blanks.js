/**
 * Fill the blanks using your understanding of `let`.
 */
function completeLetBlanks() {
  return {
    // `let` is ____ scoped.
    scopeType: "",

    // Can let be redeclared in same scope? true/false
    redeclareAllowed: null,

    // Access before declaration throws ____
    earlyAccessError: "",

    // After for(let i=0; i<3; i++) {}, is i outside loop accessible? yes/no
    loopOutsideAccess: "",
  };
}

module.exports = completeLetBlanks;
