/**
 * Fill the blanks using your understanding of `var`.
 * Keep answers lowercase where possible.
 */
function completeVarBlanks() {
  return {
    // `var` is ____ scoped.
    scopeType: "",

    // console.log(x); var x = 5; logs ____ before assignment.
    beforeAssignmentValue: "",

    // Can var be redeclared in same scope? true/false
    redeclareAllowed: null,

    // After: for (var i=0; i<3; i++) {} ; i is ____
    loopResult: "",
  };
}

module.exports = completeVarBlanks;
