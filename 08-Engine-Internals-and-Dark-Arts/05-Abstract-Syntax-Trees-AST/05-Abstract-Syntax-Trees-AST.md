# Abstract-Syntax-Trees-AST

## Concept
Imagine your codebase as a busy control room: every signal, dial, and switch has a type, a shape, and a behavior. In this exercise, you build a tiny inspector that keeps this room predictable by counting how values behave before they cause surprises.

## Challenge
Implement `solveAbstractSyntaxTreesAST` in `05-Abstract-Syntax-Trees-AST.js`.

Your function must:
- Accept an array of values as input.
- Return an object with `topic`, `total`, `truthyCount`, and `falsyCount`.
- Preserve the exact topic label `Abstract-Syntax-Trees-AST` in the `topic` field.

## Pass Condition
Run: `npm run check 08-Engine-Internals-and-Dark-Arts/05-Abstract-Syntax-Trees-AST`

## Escape Hatch
https://developer.mozilla.org/en-US/docs/Glossary/Abstract_syntax_tree
