# Currying-and-Partial-App

## Concept
Imagine your codebase as a busy control room: every signal, dial, and switch has a type, a shape, and a behavior. In this exercise, you build a tiny inspector that keeps this room predictable by counting how values behave before they cause surprises.

## Challenge
Implement `solveCurryingAndPartialApp` in `02-Currying-and-Partial-App.js`.

Your function must:
- Accept an array of values as input.
- Return an object with `topic`, `total`, `truthyCount`, and `falsyCount`.
- Preserve the exact topic label `Currying-and-Partial-App` in the `topic` field.

## Pass Condition
Run: `npm run check 07-Functional-Architecture/02-Currying-and-Partial-App`

## Escape Hatch
https://developer.mozilla.org/en-US/docs/Glossary/Currying
